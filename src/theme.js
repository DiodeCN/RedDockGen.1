// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4FC3F7',
    },
    secondary: {
      main: '#F7A8B8',
    },
  },
});

export default theme;
